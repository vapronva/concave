---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ include "convex.leaderName" . }}
  labels:
    {{- include "convex.componentLabels" (dict "ctx" . "component" "leader") | nindent 4 }}

spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "convex.componentSelectorLabels" (dict "ctx" . "component" "leader") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "convex.componentLabels" (dict "ctx" . "component" "leader") | nindent 8 }}
        {{- include "convex.backendDiscoveryLabels" (dict "ctx" . "role" "leader" "priority" .Values.ha.leaderPriority) | nindent 8 }}
    spec:
      {{- with .Values.ha.leader.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 40
      containers:
        - name: backend
          image: {{ include "convex.image" (dict "ctx" . "spec" .Values.image) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - { name: cloud, containerPort: 3210 }
            - { name: site, containerPort: 3211 }
          envFrom:
            - configMapRef:
                name: {{ include "convex.envConfigName" . }}
          env:
            - name: CONVEX_BACKEND_ROLE
              value: leader
            {{- include "convex.commonBackendEnv" . | nindent 12 }}
            {{- if .Values.funrun.enabled }}
            - name: CONVEX_FUNRUN_ADDR
              value: {{ include "convex.funrunAddr" . | quote }}
            {{- end }}
            {{- if .Values.insights.enabled }}
            {{- include "convex.usageSinkEnv" . | nindent 12 }}
            {{- end }}
          volumeMounts:
            {{- include "convex.dataVolumeMount" . | nindent 12 }}
          readinessProbe:
            httpGet: { path: /, port: cloud }
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            tcpSocket: { port: cloud }
            initialDelaySeconds: 120
            periodSeconds: 20
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "kill -INT 1; sleep 2"]
          {{- with .Values.resources.leader }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
      volumes:
        {{- include "convex.dataVolume" . | nindent 8 }}
