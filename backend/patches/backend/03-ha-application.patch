diff --git a/crates/application/src/function_log.rs b/crates/application/src/function_log.rs
index add0ec6e6..462b775c5 100644
--- a/crates/application/src/function_log.rs
+++ b/crates/application/src/function_log.rs
@@ -921,6 +921,7 @@ impl<RT: Runtime> FunctionExecutionLog<RT> {
                             user_execution_time: outcome.user_execution_time,
                             memory_in_mb: outcome.memory_in_mb,
                             occ_info: occ_info.clone(),
+                            occ_failed_permanently: occ_info.is_some() && !will_retry,
                         },
                         outcome.result.is_ok(),
                         usage_stats,
diff --git a/crates/application/src/lib.rs b/crates/application/src/lib.rs
index f81b976ac..17c49408a 100644
--- a/crates/application/src/lib.rs
+++ b/crates/application/src/lib.rs
@@ -132,6 +132,7 @@ use common::{
         env_var_total_size,
         env_var_total_size_limit_met,
         AllowedVisibility,
+        BackendRole,
         ConvexOrigin,
         ConvexSite,
         DeploymentMetadata,
@@ -589,7 +590,7 @@ pub struct Application<RT: Runtime> {
     usage_event_logger: Arc<dyn UsageEventLogger>,
     key_broker: KeyBroker,
     deployment: DeploymentMetadata,
-    workers: WorkerHandles,
+    workers: Arc<Mutex<Option<WorkerHandles>>>,
     log_visibility: Arc<dyn LogVisibility<RT>>,
     module_cache: ModuleCache<RT>,
     system_env_var_names: HashSet<EnvVarName>,
@@ -597,6 +598,10 @@ pub struct Application<RT: Runtime> {
     log_manager_client: LogManagerClient,
     audit_log_client: AuditLogClient,
     oidc_http_client: CachedHttpClient,
+    searcher: Arc<dyn Searcher>,
+    segment_term_metadata_fetcher: Arc<dyn SegmentTermMetadataFetcher>,
+    export_provider: Arc<dyn ExportProvider<RT>>,
+    fetch_client: Arc<dyn FetchClient>,
 }
 
 /// Create storage based on the storage type configuration
@@ -636,29 +641,31 @@ impl<RT: Runtime> Application<RT> {
             storage_type
         };
 
+        let storage = Self::open_application_storage(runtime, database, &storage_type).await?;
+        tracing::info!("{:?} storage is configured.", storage_type);
+        Ok(storage)
+    }
+
+    async fn open_application_storage(
+        runtime: RT,
+        database: &Database<RT>,
+        storage_type: &model::database_globals::types::StorageType,
+    ) -> anyhow::Result<ApplicationStorage> {
         let files_storage =
-            create_storage(runtime.clone(), &storage_type, StorageUseCase::Files).await?;
+            create_storage(runtime.clone(), storage_type, StorageUseCase::Files).await?;
         let modules_storage =
-            create_storage(runtime.clone(), &storage_type, StorageUseCase::Modules).await?;
-        let search_storage = create_storage(
-            runtime.clone(),
-            &storage_type,
-            StorageUseCase::SearchIndexes,
-        )
-        .await?;
+            create_storage(runtime.clone(), storage_type, StorageUseCase::Modules).await?;
+        let search_storage =
+            create_storage(runtime.clone(), storage_type, StorageUseCase::SearchIndexes).await?;
         let exports_storage =
-            create_storage(runtime.clone(), &storage_type, StorageUseCase::Exports).await?;
+            create_storage(runtime.clone(), storage_type, StorageUseCase::Exports).await?;
         let snapshot_imports_storage = create_storage(
             runtime.clone(),
-            &storage_type,
+            storage_type,
             StorageUseCase::SnapshotImports,
         )
         .await?;
-
-        // Search storage needs to be set for Database to be fully initialized
         database.set_search_storage(search_storage.clone());
-        tracing::info!("{:?} storage is configured.", storage_type);
-
         Ok(ApplicationStorage {
             files_storage,
             modules_storage,
@@ -668,7 +675,34 @@ impl<RT: Runtime> Application<RT> {
         })
     }
 
+    pub async fn initialize_storage_readonly(
+        runtime: RT,
+        database: &Database<RT>,
+    ) -> anyhow::Result<ApplicationStorage> {
+        let storage_type = {
+            let mut tx = database.begin_system().await?;
+            DatabaseGlobalsModel::new(&mut tx)
+                .database_globals()
+                .await?
+                .into_value()
+                .storage_type
+                .ok_or_else(|| {
+                    anyhow::anyhow!(
+                        "follower cannot start: database storage tag is not initialized (the \
+                         leader must initialize the database first)"
+                    )
+                })?
+        };
+        let storage = Self::open_application_storage(runtime, database, &storage_type).await?;
+        tracing::info!(
+            "{:?} storage is configured (follower, read-only).",
+            storage_type
+        );
+        Ok(storage)
+    }
+
     pub async fn new(
+        role: BackendRole,
         runtime: RT,
         database: Database<RT>,
         file_storage: FileStorage<RT>,
@@ -681,7 +715,7 @@ impl<RT: Runtime> Application<RT> {
         convex_site: ConvexSite,
         searcher: Arc<dyn Searcher>,
         segment_term_metadata_fetcher: Arc<dyn SegmentTermMetadataFetcher>,
-        persistence: Arc<dyn Persistence>,
+        persistence: Option<Arc<dyn Persistence>>,
         node_actions: Actions<RT>,
         log_visibility: Arc<dyn LogVisibility<RT>>,
         app_auth: Arc<ApplicationAuth<RT>>,
@@ -704,6 +738,145 @@ impl<RT: Runtime> Application<RT> {
             CONVEX_SITE.clone() => convex_site.parse()?
         };
 
+        let usage_counter = UsageCounter::new(usage_event_logger.clone());
+        let log_manager_client = if role.is_leader() {
+            let log_streaming_allowed = match &local_log_sink {
+                Some(path) => {
+                    add_local_log_sink_on_startup(database.clone(), path.clone()).await?;
+                    true
+                },
+                _ => {
+                    let mut tx = database.begin(Identity::system()).await?;
+                    let mut bi = BackendInfoModel::new(&mut tx);
+                    bi.is_log_streaming_allowed().await?
+                },
+            };
+            LogManager::start(
+                runtime.clone(),
+                database.clone(),
+                fetch_client.clone(),
+                deployment_name.clone(),
+                deployment_region.as_ref().map(|r| r.to_string()),
+                log_streaming_allowed,
+                usage_counter.clone(),
+            )
+            .await
+        } else {
+            tracing::info!(
+                "[backend-role] follower: using no-op LogManagerClient (log streaming disabled, \
+                 no DB writes)"
+            );
+            LogManagerClient::noop()
+        };
+        let (is_dev_deployment, firehose_stream_name) = {
+            let mut tx = database.begin(Identity::system()).await?;
+            let is_dev_deployment = if let Some(doc) = BackendInfoModel::new(&mut tx).get().await? {
+                doc.deployment_type == DeploymentType::Dev
+            } else {
+                false
+            };
+            let firehose_stream_name = AuditLogConfigModel::new(&mut tx)
+                .get_or_create()
+                .await?
+                .firehose_stream_name
+                .clone();
+            (is_dev_deployment, firehose_stream_name)
+        };
+        let audit_log_client = AuditLogClient::new(
+            log_manager_client.clone(),
+            is_dev_deployment,
+            firehose_stream_name,
+            &deployment.name,
+        )
+        .await?;
+        let function_log = FunctionExecutionLog::new(
+            runtime.clone(),
+            usage_counter.clone(),
+            Arc::new(log_manager_client.clone()),
+        );
+        {
+            let function_log = function_log.clone();
+            database.set_invalidation_callback(Arc::new(move |events| {
+                function_log.record_subscription_invalidations(events);
+            }))?;
+        }
+        let runner = Arc::new(ApplicationFunctionRunner::new(
+            runtime.clone(),
+            database.clone(),
+            key_broker.clone(),
+            function_runner.clone(),
+            node_actions,
+            file_storage.transactional_file_storage.clone(),
+            application_storage.modules_storage.clone(),
+            module_loader,
+            function_log.clone(),
+            audit_log_client.clone(),
+            default_system_env_vars.clone(),
+            cache,
+        ));
+        function_runner.set_action_callbacks(runner.clone());
+        let application = Self {
+            runtime: runtime.clone(),
+            database: database.clone(),
+            runner,
+            function_log,
+            file_storage,
+            application_storage,
+            usage_event_logger,
+            usage_counter,
+            key_broker,
+            deployment,
+            workers: Arc::new(Mutex::new(None)),
+            log_visibility,
+            module_cache,
+            system_env_var_names: default_system_env_vars.into_keys().collect(),
+            app_auth,
+            log_manager_client: log_manager_client.clone(),
+            audit_log_client,
+            oidc_http_client,
+            searcher,
+            segment_term_metadata_fetcher,
+            export_provider,
+            fetch_client: fetch_client.clone(),
+        };
+        if role.is_leader() {
+            let persistence = persistence
+                .clone()
+                .expect("leader requires a read-write Persistence handle");
+            application
+                .start_leader_workers(
+                    persistence,
+                    log_manager_client,
+                    lease_lost_shutdown,
+                    deleted_tablet_receiver,
+                )
+                .await?;
+        } else {
+            tracing::info!(
+                "[backend-role] follower mode: skipping all write-path workers (warm standby); \
+                 awaiting in-process promotion"
+            );
+            let _ = (lease_lost_shutdown, deleted_tablet_receiver, persistence);
+        }
+        Ok(application)
+    }
+
+    pub(crate) async fn start_leader_workers(
+        &self,
+        persistence: Arc<dyn Persistence>,
+        log_manager_client: LogManagerClient,
+        lease_lost_shutdown: ShutdownSignal,
+        deleted_tablet_receiver: tokio::sync::mpsc::Receiver<TabletId>,
+    ) -> anyhow::Result<()> {
+        let runtime = &self.runtime;
+        let database = &self.database;
+        let application_storage = &self.application_storage;
+        let usage_event_logger = &self.usage_event_logger;
+        let usage_counter = &self.usage_counter;
+        let function_log = &self.function_log;
+        let runner = &self.runner;
+        let file_storage = &self.file_storage;
+        let deployment_name = self.deployment.name.clone();
         let mut index_worker = Arc::new(Mutex::new(None));
         if *ENABLE_INDEX_BACKFILL {
             let index_worker_fut = IndexWorker::new(
@@ -728,12 +901,12 @@ impl<RT: Runtime> Application<RT> {
             database.clone(),
             persistence.reader(),
             application_storage.search_storage.clone(),
-            searcher,
-            segment_term_metadata_fetcher,
+            self.searcher.clone(),
+            self.segment_term_metadata_fetcher.clone(),
         );
         let search_worker = Arc::new(Mutex::new(search_worker));
         let search_and_vector_bootstrap_worker =
-            Arc::new(Mutex::new(database.start_search_and_vector_bootstrap()));
+            Arc::new(Mutex::new(database.start_search_and_vector_bootstrap()?));
         let table_summary_worker = TableSummaryWorker::start(
             runtime.clone(),
             database.clone(),
@@ -755,74 +928,6 @@ impl<RT: Runtime> Application<RT> {
             runtime.spawn("system_table_cleanup_worker", system_table_cleanup_worker),
         ));
 
-        // If local_log_sink is passed in, this is a local instance, so we enable log
-        // streaming by default. Otherwise, it's hard to grant the
-        // entitlement in testing and in load generator. If not local, we
-        // read the entitlement from the database.
-        let mut tx = database.begin(Identity::system()).await?;
-        let mut bi = BackendInfoModel::new(&mut tx);
-        let log_streaming_allowed = if let Some(path) = local_log_sink {
-            add_local_log_sink_on_startup(database.clone(), path).await?;
-            true
-        } else {
-            bi.is_log_streaming_allowed().await?
-        };
-
-        let usage_counter = UsageCounter::new(usage_event_logger.clone());
-        let log_manager_client = LogManager::start(
-            runtime.clone(),
-            database.clone(),
-            fetch_client.clone(),
-            deployment_name.clone(),
-            deployment_region.as_ref().map(|r| r.to_string()),
-            log_streaming_allowed,
-            usage_counter.clone(),
-        )
-        .await;
-
-        let is_dev_deployment = if let Some(doc) = bi.get().await? {
-            doc.deployment_type == DeploymentType::Dev
-        } else {
-            false
-        };
-        let audit_log_config = AuditLogConfigModel::new(&mut tx).get_or_create().await?;
-        let audit_log_client = AuditLogClient::new(
-            log_manager_client.clone(),
-            is_dev_deployment,
-            audit_log_config.firehose_stream_name.clone(),
-            &deployment.name,
-        )
-        .await?;
-
-        let function_log = FunctionExecutionLog::new(
-            runtime.clone(),
-            usage_counter.clone(),
-            Arc::new(log_manager_client.clone()),
-        );
-
-        {
-            let function_log = function_log.clone();
-            database.set_invalidation_callback(Arc::new(move |events| {
-                function_log.record_subscription_invalidations(events);
-            }))?;
-        }
-
-        let runner = Arc::new(ApplicationFunctionRunner::new(
-            runtime.clone(),
-            database.clone(),
-            key_broker.clone(),
-            function_runner.clone(),
-            node_actions,
-            file_storage.transactional_file_storage.clone(),
-            application_storage.modules_storage.clone(),
-            module_loader,
-            function_log.clone(),
-            audit_log_client.clone(),
-            default_system_env_vars.clone(),
-            cache,
-        ));
-        function_runner.set_action_callbacks(runner.clone());
-
         let scheduled_job_runner = ScheduledJobRunner::start(
             runtime.clone(),
             deployment_name.clone(),
@@ -847,7 +952,7 @@ impl<RT: Runtime> Application<RT> {
             database.clone(),
             application_storage.exports_storage.clone(),
             application_storage.files_storage.clone(),
-            export_provider,
+            self.export_provider.clone(),
             usage_counter.clone(),
             deployment_name.clone(),
         );
@@ -884,7 +989,7 @@ impl<RT: Runtime> Application<RT> {
             deployment_name.clone(),
         );
 
-        let workers = WorkerHandles {
+        let handles = WorkerHandles {
             usage_gauges_tracking_worker,
             scheduled_job_runner,
             cron_job_executor,
@@ -899,33 +1004,80 @@ impl<RT: Runtime> Application<RT> {
             system_table_cleanup_worker,
             migration_worker,
         };
+        *self.workers.lock() = Some(handles);
+        Ok(())
+    }
 
-        Ok(Self {
-            runtime,
-            database,
-            runner,
-            function_log,
-            file_storage,
-            application_storage,
-            usage_event_logger,
-            usage_counter,
-            key_broker,
-            deployment,
-            workers,
-            log_visibility,
-            module_cache,
-            system_env_var_names: default_system_env_vars.into_keys().collect(),
-            app_auth,
-            log_manager_client,
-            audit_log_client,
-            oidc_http_client,
-        })
+    pub async fn become_leader(
+        &self,
+        rw_persistence: Arc<dyn Persistence>,
+        retention_rate_limiter: Arc<common::runtime::RateLimiter<RT>>,
+        preempt_signal: ShutdownSignal,
+    ) -> anyhow::Result<()> {
+        tracing::info!("[promote] Application::become_leader: promoting Database");
+        let deleted_tablet_receiver = self
+            .database
+            .become_leader(
+                rw_persistence.clone(),
+                retention_rate_limiter,
+                preempt_signal.clone(),
+            )
+            .await?;
+        let log_streaming_allowed = {
+            let mut tx = self.database.begin(Identity::system()).await?;
+            let mut bi = BackendInfoModel::new(&mut tx);
+            bi.is_log_streaming_allowed().await?
+        };
+        let real_log_manager_client = LogManager::start(
+            self.runtime.clone(),
+            self.database.clone(),
+            self.fetch_client.clone(),
+            self.deployment.name.clone(),
+            self.deployment.region.as_ref().map(|r| r.to_string()),
+            log_streaming_allowed,
+            self.usage_counter.clone(),
+        )
+        .await;
+        self.log_manager_client
+            .replace_with(&real_log_manager_client);
+        tracing::info!("[promote] Application::become_leader: starting leader workers");
+        self.start_leader_workers(
+            rw_persistence,
+            real_log_manager_client,
+            preempt_signal,
+            deleted_tablet_receiver,
+        )
+        .await?;
+        tracing::info!("[promote] Application::become_leader complete");
+        Ok(())
+    }
+
+    pub async fn become_follower(
+        &self,
+        rw_persistence: Arc<dyn Persistence>,
+    ) -> anyhow::Result<()> {
+        tracing::info!("[demote] Application::become_follower: stopping leader workers");
+        let workers = self.workers.lock().take();
+        if let Some(workers) = workers {
+            workers.shutdown().await?;
+        }
+        let _ = self.log_manager_client.shutdown().await;
+        self.log_manager_client
+            .replace_with(&LogManagerClient::noop());
+        tracing::info!("[demote] Application::become_follower: demoting Database");
+        self.database.become_follower(rw_persistence).await?;
+        tracing::info!("[demote] Application::become_follower complete");
+        Ok(())
     }
 
     pub fn runtime(&self) -> RT {
         self.runtime.clone()
     }
 
+    pub fn database(&self) -> &Database<RT> {
+        &self.database
+    }
+
     pub fn modules_storage(&self) -> &Arc<dyn Storage> {
         &self.application_storage.modules_storage
     }
@@ -3700,7 +3852,10 @@ impl<RT: Runtime> Application<RT> {
     }
 
     pub async fn shutdown(&self) -> anyhow::Result<()> {
-        self.workers.shutdown().await?;
+        let workers = self.workers.lock().clone();
+        if let Some(workers) = &workers {
+            workers.shutdown().await?;
+        }
         self.log_manager_client.shutdown().await?;
         self.runner.shutdown().await?;
         self.database.shutdown().await?;
diff --git a/crates/log_streaming/src/lib.rs b/crates/log_streaming/src/lib.rs
index 6ff4d3f66..e11ddf229 100644
--- a/crates/log_streaming/src/lib.rs
+++ b/crates/log_streaming/src/lib.rs
@@ -94,6 +94,11 @@ use crate::sinks::{
 /// See `log_streaming/README.md` for more info.
 #[derive(Clone)]
 pub struct LogManagerClient {
+    inner: Arc<RwLock<LogManagerClientInner>>,
+}
+
+#[derive(Clone)]
+struct LogManagerClientInner {
     handle: Arc<Mutex<Option<Box<dyn SpawnHandle>>>>,
     event_sender: mpsc::Sender<LogEvent>,
     active_sinks_count: Arc<AtomicUsize>,
@@ -101,12 +106,33 @@ pub struct LogManagerClient {
 }
 
 impl LogManagerClient {
+    pub fn noop() -> Self {
+        let (event_sender, _rx) = mpsc::channel(1);
+        LogManagerClient::from_inner(LogManagerClientInner {
+            handle: Arc::new(Mutex::new(None)),
+            event_sender,
+            active_sinks_count: Arc::new(AtomicUsize::new(0)),
+            entitlement_enabled: Arc::new(AtomicBool::new(false)),
+        })
+    }
+
+    fn from_inner(inner: LogManagerClientInner) -> Self {
+        Self {
+            inner: Arc::new(RwLock::new(inner)),
+        }
+    }
+
+    pub fn replace_with(&self, other: &Self) {
+        *self.inner.write() = other.inner.read().clone();
+    }
+
     fn send_logs_inner(&self, logs: Vec<LogEvent>) -> Result<(), SendLogBatchError> {
         let mut dropped = 0;
         let mut num_left = logs.len();
         let total = logs.len();
+        let event_sender = self.inner.read().event_sender.clone();
         for log in logs.into_iter() {
-            match self.event_sender.try_send(log) {
+            match event_sender.try_send(log) {
                 Err(mpsc::error::TrySendError::Full(_)) => {
                     dropped += 1;
                 },
@@ -133,15 +159,21 @@ impl LogManagerClient {
     }
 
     pub fn set_entitlement_enabled(&self, enabled: bool) {
-        self.entitlement_enabled.store(enabled, Ordering::Relaxed)
+        self.inner
+            .read()
+            .entitlement_enabled
+            .store(enabled, Ordering::Relaxed)
     }
 
     fn get_active_sinks_count(&self) -> usize {
-        self.active_sinks_count.load(Ordering::Relaxed)
+        self.inner.read().active_sinks_count.load(Ordering::Relaxed)
     }
 
     fn is_entitlement_enabled(&self) -> bool {
-        self.entitlement_enabled.load(Ordering::Relaxed)
+        self.inner
+            .read()
+            .entitlement_enabled
+            .load(Ordering::Relaxed)
     }
 
     fn is_active(&self) -> bool {
@@ -152,7 +184,7 @@ impl LogManagerClient {
 #[async_trait]
 impl LogSender for LogManagerClient {
     async fn shutdown(&self) -> anyhow::Result<()> {
-        let handle = self.handle.lock().take();
+        let handle = self.inner.read().handle.lock().take();
         if let Some(handle) = handle {
             shutdown_and_join(handle).await?;
         }
@@ -276,12 +308,12 @@ impl<RT: Runtime> LogManager<RT> {
 
         let handle = Arc::new(Mutex::new(Some(runtime.spawn("log_manager", worker.go()))));
         let entitlement_enabled = Arc::new(AtomicBool::new(entitlement_enabled));
-        LogManagerClient {
+        LogManagerClient::from_inner(LogManagerClientInner {
             handle,
             event_sender: req_tx,
             active_sinks_count,
             entitlement_enabled,
-        }
+        })
     }
 
     /// Event loop
