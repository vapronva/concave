diff --git a/crates/db_connection/src/lib.rs b/crates/db_connection/src/lib.rs
index 16d0aea04..6b49a4cb0 100644
--- a/crates/db_connection/src/lib.rs
+++ b/crates/db_connection/src/lib.rs
@@ -232,6 +232,39 @@ pub async fn connect_persistence_reader<RT: Runtime>(
     }
 }
 
+pub async fn postgres_database_initialized<RT: Runtime>(
+    db: DbDriverTag,
+    db_spec: &str,
+    require_ssl: bool,
+    deployment_name: &str,
+    runtime: RT,
+) -> anyhow::Result<bool> {
+    match persistence_seed(
+        db,
+        db_spec,
+        ConnectPersistenceFlags {
+            require_ssl,
+            allow_read_only: true,
+            skip_index_creation: false,
+        },
+        deployment_name,
+        runtime,
+    )? {
+        PersistenceSeed::Postgres { config, options } => {
+            let pool = PostgresPersistence::create_pool(config)
+                .context("failed to create postgres pool")?;
+            let reader_options = PostgresReaderOptions {
+                version: options.version,
+                schema: options.schema,
+                instance_name: options.instance_name,
+                multitenant: options.multitenant,
+            };
+            PostgresPersistence::schema_initialized(pool, reader_options).await
+        },
+        _ => anyhow::bail!("the boot-role probe requires a Postgres persistence backend"),
+    }
+}
+
 pub async fn set_read_only<RT: Runtime>(
     db: DbDriverTag,
     db_spec: &str,
diff --git a/crates/local_backend/src/authentication.rs b/crates/local_backend/src/authentication.rs
index f280d1494..9208b0c0a 100644
--- a/crates/local_backend/src/authentication.rs
+++ b/crates/local_backend/src/authentication.rs
@@ -12,10 +12,7 @@ use axum::{
 };
 use common::{
     http::{
-        extract::{
-            FromMtState,
-            Query,
-        },
+        extract::FromMtState,
         ExtractRequestId,
         ExtractRequestMetadata,
         ExtractResolvedHostname,
@@ -27,7 +24,6 @@ use common::{
 };
 use errors::ErrorMetadata;
 use keybroker::Identity;
-use serde::Deserialize;
 use sync_types::{
     AuthenticationToken,
     UserIdentityAttributes,
@@ -80,19 +76,6 @@ impl<T: Sync> FromRequestParts<T> for ExtractAuthenticationToken {
             };
         }
 
-        // If no header is provided, also allow extracting admin key from query param.
-        #[derive(Deserialize)]
-        #[serde(rename_all = "camelCase")]
-        struct QueryParams {
-            admin_key: Option<String>,
-        }
-        if let Query(QueryParams {
-            admin_key: Some(admin_key),
-        }) = parts.extract().await?
-        {
-            return Ok(Self(AuthenticationToken::Admin(admin_key, None)));
-        }
-
         Ok(Self(AuthenticationToken::None))
     }
 }
@@ -212,3 +195,36 @@ fn strip_prefix_ignore_case<'a>(string: &'a str, prefix: &str) -> Option<&'a str
         None
     }
 }
+
+#[cfg(test)]
+mod tests {
+    use axum::extract::FromRequestParts;
+    use sync_types::AuthenticationToken;
+
+    use super::ExtractAuthenticationToken;
+
+    async fn extract(
+        uri: &str,
+    ) -> Result<ExtractAuthenticationToken, common::http::HttpResponseError> {
+        let request = http::Request::builder().uri(uri).body(()).unwrap();
+        let (mut parts, _) = request.into_parts();
+        ExtractAuthenticationToken::from_request_parts(&mut parts, &()).await
+    }
+
+    #[tokio::test]
+    async fn admin_key_query_parameter_is_ignored() {
+        let ExtractAuthenticationToken(token) =
+            extract("/api/query?adminKey=secret").await.unwrap();
+        assert!(matches!(token, AuthenticationToken::None));
+        let ExtractAuthenticationToken(token) = extract("/api/export/zip/123?adminKey=secret")
+            .await
+            .unwrap();
+        assert!(matches!(token, AuthenticationToken::None));
+    }
+
+    #[tokio::test]
+    async fn no_credentials_is_none() {
+        let ExtractAuthenticationToken(token) = extract("/api/query").await.unwrap();
+        assert!(matches!(token, AuthenticationToken::None));
+    }
+}
diff --git a/crates/local_backend/src/lib.rs b/crates/local_backend/src/lib.rs
index 4633f83f5..1c5dd565c 100644
--- a/crates/local_backend/src/lib.rs
+++ b/crates/local_backend/src/lib.rs
@@ -33,7 +33,10 @@ use common::{
         INDEX_CACHE_SIZE,
         UDF_CACHE_MAX_SIZE,
     },
-    persistence::Persistence,
+    persistence::{
+        Persistence,
+        PersistenceReader,
+    },
     runtime::{
         new_rate_limiter,
         Runtime,
@@ -49,6 +52,10 @@ use common::{
 };
 use config::LocalConfig;
 use database::Database;
+use db_connection::{
+    connect_persistence,
+    ConnectPersistenceFlags,
+};
 use events::usage::NoOpUsageEventLogger;
 use exports::interface::InProcessExportProvider;
 use file_storage::{
@@ -94,6 +101,7 @@ pub mod deployment_info;
 pub mod deployment_state;
 pub mod environment_variables;
 pub mod http_actions;
+pub mod leadership;
 pub mod log_sinks;
 pub mod logs;
 pub mod node_action_callbacks;
@@ -109,7 +117,7 @@ pub mod storage;
 pub mod streaming_export;
 pub mod streaming_import;
 pub mod subs;
-pub const MAX_CONCURRENT_REQUESTS: usize = 128;
+pub mod usage_forwarder;
 
 #[derive(Clone)]
 pub struct LocalAppState {
@@ -119,6 +127,12 @@ pub struct LocalAppState {
     pub site_origin: ConvexSite,
     // Name of the instance. (e.g. crazy-giraffe-123)
     pub instance_name: String,
+    pub role: Arc<crate::leadership::RoleState>,
+    pub configured_role: common::types::BackendRole,
+    pub promote_ctx: Arc<crate::leadership::PromoteContext>,
+    pub leader_handle: crate::leadership::LeaderHandle,
+    pub control_plane_token: Option<Arc<String>>,
+    pub insights_proxy: Option<crate::usage_forwarder::InsightsProxy>,
     pub application: Application<ProdRuntime>,
     pub zombify_rx: async_broadcast::Receiver<()>,
 }
@@ -143,10 +157,31 @@ pub struct RouterState {
 #[derive(Serialize)]
 pub struct EmptyResponse {}
 
+pub enum RolePersistence {
+    Leader(Arc<dyn Persistence>),
+    Follower(Arc<dyn PersistenceReader>),
+}
+
+impl RolePersistence {
+    pub fn reader(&self) -> Arc<dyn PersistenceReader> {
+        match self {
+            RolePersistence::Leader(p) => p.reader(),
+            RolePersistence::Follower(r) => r.clone(),
+        }
+    }
+
+    pub fn as_persistence(&self) -> Option<Arc<dyn Persistence>> {
+        match self {
+            RolePersistence::Leader(p) => Some(p.clone()),
+            RolePersistence::Follower(_) => None,
+        }
+    }
+}
+
 pub async fn make_app(
     runtime: ProdRuntime,
     config: LocalConfig,
-    persistence: Arc<dyn Persistence>,
+    persistence: RolePersistence,
     zombify_rx: async_broadcast::Receiver<()>,
     preempt_tx: ShutdownSignal,
 ) -> anyhow::Result<LocalAppState> {
@@ -156,29 +191,70 @@ pub async fn make_app(
     // TODO(CX-6572) Separate `SegmentMetadataFetcher` from `SearcherImpl`
     let segment_metadata_fetcher: Arc<dyn SegmentTermMetadataFetcher> = in_process_searcher;
     let (deleted_tablet_sender, deleted_tablet_receiver) = tokio::sync::mpsc::channel(100);
-    let usage_event_logger = Arc::new(NoOpUsageEventLogger);
-    let database = Database::load(
-        persistence.clone(),
-        runtime.clone(),
-        searcher.clone(),
-        preempt_tx.clone(),
-        virtual_system_mapping().clone(),
-        IndexCache::new(*INDEX_CACHE_SIZE).new_handle(),
-        Arc::new(new_rate_limiter(
+    let usage_event_logger: Arc<dyn events::usage::UsageEventLogger> =
+        match std::env::var("CONVEX_USAGE_SINK_URL") {
+            Ok(url) if !url.is_empty() => {
+                tracing::info!("[concave] forwarding usage events to {url}");
+                Arc::new(crate::usage_forwarder::HttpUsageForwarder::new(
+                    url,
+                    config.name(),
+                )?)
+            },
+            _ => Arc::new(NoOpUsageEventLogger),
+        };
+    let boot_role = match &persistence {
+        RolePersistence::Leader(_) => common::types::BackendRole::Leader,
+        RolePersistence::Follower(_) => common::types::BackendRole::Follower,
+    };
+    let database = match &persistence {
+        RolePersistence::Leader(persistence) => {
+            Database::load(
+                persistence.clone(),
+                runtime.clone(),
+                searcher.clone(),
+                preempt_tx.clone(),
+                virtual_system_mapping().clone(),
+                IndexCache::new(*INDEX_CACHE_SIZE).new_handle(),
+                Arc::new(new_rate_limiter(
+                    runtime.clone(),
+                    Quota::per_second(*DOCUMENT_RETENTION_RATE_LIMIT),
+                )),
+                deleted_tablet_sender,
+            )
+            .await?
+        },
+        RolePersistence::Follower(reader) => {
+            tracing::info!("[concave] starting in FOLLOWER mode (read-only, no lease)");
+            drop(deleted_tablet_sender);
+            Database::load_follower(
+                reader.clone(),
+                runtime.clone(),
+                searcher.clone(),
+                virtual_system_mapping().clone(),
+                IndexCache::new(*INDEX_CACHE_SIZE).new_handle(),
+            )
+            .await?
+        },
+    };
+    if config.role.is_leader() && std::env::var_os("CONVEX_BACKEND_ROLE").is_none() {
+        tracing::warn!(
+            "[concave] CONVEX_BACKEND_ROLE unset; an HA standby must set it to follower"
+        );
+    }
+    if boot_role.is_leader() {
+        initialize_application_system_tables(&database).await?;
+    }
+    let application_storage = if boot_role.is_leader() {
+        Application::initialize_storage(
             runtime.clone(),
-            Quota::per_second(*DOCUMENT_RETENTION_RATE_LIMIT),
-        )),
-        deleted_tablet_sender,
-    )
-    .await?;
-    initialize_application_system_tables(&database).await?;
-    let application_storage = Application::initialize_storage(
-        runtime.clone(),
-        &database,
-        config.storage_tag_initializer(),
-        config.name(),
-    )
-    .await?;
+            &database,
+            config.storage_tag_initializer(),
+            config.name(),
+        )
+        .await?
+    } else {
+        Application::initialize_storage_readonly(runtime.clone(), &database).await?
+    };
 
     let file_storage = FileStorage {
         transactional_file_storage: TransactionalFileStorage::new(
@@ -221,21 +297,41 @@ pub async fn make_app(
         reqwest::redirect::Policy::default(),
     );
     let function_runner: Arc<dyn FunctionRunner<ProdRuntime>> =
-        Arc::new(InProcessFunctionRunner::new(
-            deployment,
-            key_broker.function_runner_keybroker(),
-            config.convex_origin_url()?,
-            runtime.clone(),
-            persistence.reader(),
-            DeploymentStorage {
-                files_storage: application_storage.files_storage.clone(),
-                modules_storage: application_storage.modules_storage.clone(),
+        match std::env::var("CONVEX_FUNRUN_ADDR") {
+            Ok(funrun_addr) if !funrun_addr.is_empty() => {
+                tracing::info!(
+                    "[funrun] offloading UDF execution to remote funrun fleet at {funrun_addr}"
+                );
+                Arc::new(function_runner::remote::RemoteFunctionRunner::new(
+                    funrun_addr,
+                    deployment,
+                    key_broker.clone(),
+                    config.secret()?,
+                    config.convex_origin_url()?,
+                    runtime.clone(),
+                    DeploymentStorage {
+                        files_storage: application_storage.files_storage.clone(),
+                        modules_storage: application_storage.modules_storage.clone(),
+                    },
+                )?)
             },
-            database.clone(),
-            fetch_client.clone(),
-        )?);
+            _ => Arc::new(InProcessFunctionRunner::new(
+                deployment,
+                key_broker.function_runner_keybroker(),
+                config.convex_origin_url()?,
+                runtime.clone(),
+                persistence.reader(),
+                DeploymentStorage {
+                    files_storage: application_storage.files_storage.clone(),
+                    modules_storage: application_storage.modules_storage.clone(),
+                },
+                database.clone(),
+                fetch_client.clone(),
+            )?),
+        };
 
     let application = Application::new(
+        boot_role,
         runtime.clone(),
         database.clone(),
         file_storage.clone(),
@@ -252,7 +348,7 @@ pub async fn make_app(
         config.convex_site_url()?,
         searcher.clone(),
         segment_metadata_fetcher,
-        persistence,
+        persistence.as_persistence(),
         actions,
         Arc::new(RedactLogsToClient::new(config.redact_logs_to_client)),
         Arc::new(ApplicationAuth::new(
@@ -273,7 +369,7 @@ pub async fn make_app(
     let origin = config.convex_origin_url()?;
     let instance_name = config.name();
 
-    if !config.disable_beacon {
+    if !config.disable_beacon && boot_role.is_leader() {
         let beacon_future = beacon::start_beacon(
             runtime.clone(),
             database.clone(),
@@ -283,14 +379,100 @@ pub async fn make_app(
         runtime.spawn_background("beacon_worker", beacon_future);
     }
 
+    let role = Arc::new(crate::leadership::RoleState::new(boot_role));
+    let promote_ctx = {
+        let factory_db = config.db;
+        let factory_db_spec = config.db_spec.clone();
+        let factory_require_ssl = !config.do_not_require_ssl;
+        let factory_name = config.name();
+        let factory_runtime = runtime.clone();
+        let factory_preempt = preempt_tx.clone();
+        let persistence_factory: crate::leadership::PersistenceFactory = Arc::new(move || {
+            let db = factory_db;
+            let db_spec = factory_db_spec.clone();
+            let name = factory_name.clone();
+            let runtime = factory_runtime.clone();
+            let preempt = factory_preempt.clone();
+            Box::pin(async move {
+                let persistence = connect_persistence(
+                    db,
+                    &db_spec,
+                    ConnectPersistenceFlags {
+                        require_ssl: factory_require_ssl,
+                        allow_read_only: false,
+                        skip_index_creation: false,
+                    },
+                    &name,
+                    runtime,
+                    preempt,
+                )
+                .await?;
+                anyhow::ensure!(
+                    persistence.supports_lease(),
+                    "HA promotion requires a lease-capable persistence backend (Postgres); this \
+                     backend does not support the lease primitive, so single-writer fencing would \
+                     be a no-op"
+                );
+                Ok(persistence)
+            })
+        });
+        Arc::new(crate::leadership::PromoteContext {
+            persistence_factory,
+            preempt_signal: preempt_tx.clone(),
+            retention_rate_limiter: Arc::new(new_rate_limiter(
+                runtime.clone(),
+                Quota::per_second(*DOCUMENT_RETENTION_RATE_LIMIT),
+            )),
+        })
+    };
+    let leader_handle = match persistence.as_persistence() {
+        Some(rw_persistence) => {
+            crate::leadership::LeaderHandle::for_leader(rw_persistence, preempt_tx.clone())
+        },
+        None => crate::leadership::LeaderHandle::for_follower(preempt_tx.clone()),
+    };
+    let control_plane_token = std::env::var("CONVEX_CONTROL_PLANE_TOKEN")
+        .ok()
+        .filter(|t| !t.is_empty())
+        .map(Arc::new);
+    if control_plane_token.is_some() {
+        tracing::info!(
+            "[concave] CONVEX_CONTROL_PLANE_TOKEN set; /instance/promote and /instance/demote \
+             require the X-Convex-Control-Plane-Token header"
+        );
+    } else {
+        tracing::warn!(
+            "[concave] CONVEX_CONTROL_PLANE_TOKEN is unset; all /instance control-plane requests \
+             will be rejected"
+        );
+    }
     let app_state = LocalAppState {
         origin,
         site_origin: config.convex_site_url()?,
         instance_name,
+        role,
+        configured_role: config.role,
+        promote_ctx,
+        leader_handle: leader_handle.clone(),
+        control_plane_token,
+        insights_proxy: crate::usage_forwarder::InsightsProxy::from_env()?,
         application,
         zombify_rx,
     };
 
+    if let Some(rw_persistence) = leader_handle.rw_persistence() {
+        if rw_persistence.supports_lease() {
+            crate::leadership::install_lease_lost_handler(&app_state, rw_persistence.clone());
+            app_state
+                .application
+                .database()
+                .spawn_lease_watch(rw_persistence, preempt_tx.clone());
+        } else {
+            tracing::info!(
+                "[concave] no lease primitive; skipping lease-watch (commit-time fencing)"
+            );
+        }
+    }
     Ok(app_state)
 }
 
diff --git a/crates/local_backend/src/main.rs b/crates/local_backend/src/main.rs
index 4217d331d..b508e24e8 100644
--- a/crates/local_backend/src/main.rs
+++ b/crates/local_backend/src/main.rs
@@ -12,11 +12,16 @@ use common::{
     runtime::Runtime,
     sentry::set_sentry_tags,
     shutdown::ShutdownSignal,
-    types::MemberId,
+    types::{
+        BackendRole,
+        MemberId,
+    },
     version::SERVER_VERSION_STR,
 };
 use db_connection::{
     connect_persistence,
+    connect_persistence_reader,
+    postgres_database_initialized,
     ConnectPersistenceFlags,
 };
 use futures::{
@@ -37,11 +42,11 @@ use local_backend::{
         LocalConfig,
         Subcommand,
     },
+    leadership::follower_router,
     make_app,
     proxy::dev_site_proxy,
-    router::router,
     HttpActionRouteMapper,
-    MAX_CONCURRENT_REQUESTS,
+    RolePersistence,
 };
 use runtime::prod::ProdRuntime;
 use tokio::{
@@ -51,6 +56,21 @@ use tokio::{
     sync::oneshot,
 };
 
+async fn shutdown_signal() -> std::io::Result<()> {
+    #[cfg(unix)]
+    {
+        let mut term = signal::unix::signal(signal::unix::SignalKind::terminate())?;
+        tokio::select! {
+            r = signal::ctrl_c() => r,
+            _ = term.recv() => Ok(()),
+        }
+    }
+    #[cfg(not(unix))]
+    {
+        signal::ctrl_c().await
+    }
+}
+
 fn main() -> Result<(), MainError> {
     let config = LocalConfig::parse();
     if let Some(subcommand) = &config.subcommand {
@@ -137,24 +157,74 @@ async fn run_server(runtime: ProdRuntime, config: LocalConfig) -> anyhow::Result
 }
 
 async fn run_server_inner(runtime: ProdRuntime, config: LocalConfig) -> anyhow::Result<()> {
+    let dynamic_roles_enabled = matches!(config.role, BackendRole::Follower)
+        || std::env::var("CONVEX_CONTROL_PLANE_TOKEN").is_ok_and(|token| !token.is_empty());
+    if dynamic_roles_enabled {
+        anyhow::ensure!(
+            matches!(
+                config.db,
+                clusters::DbDriverTag::Postgres(_) | clusters::DbDriverTag::PostgresMultitenant(_)
+            ),
+            "dynamic backend roles require Postgres because MySQL/SQLite do not implement the HA \
+             lease fencing contract"
+        );
+    }
+    if config.boot_follower_when_initialized && !dynamic_roles_enabled {
+        tracing::warn!(
+            "[concave] CONVEX_BOOT_FOLLOWER_WHEN_INITIALIZED is set but dynamic roles are \
+             disabled; booting with the static role"
+        );
+    }
+    let boot_as_follower = config.role.is_leader()
+        && config.boot_follower_when_initialized
+        && dynamic_roles_enabled
+        && postgres_database_initialized(
+            config.db,
+            &config.db_spec,
+            !config.do_not_require_ssl,
+            &config.name(),
+            runtime.clone(),
+        )
+        .await?;
+    if boot_as_follower {
+        tracing::info!(
+            "[concave] database already initialized; booting as follower and awaiting promotion"
+        );
+    }
     // Used to receive fatal errors from the database or /preempt endpoint.
     let (preempt_tx, preempt_rx) = oneshot::channel();
     let preempt_signal = ShutdownSignal::new(preempt_tx);
     // Use to signal to the http service to stop.
     let (shutdown_tx, shutdown_rx) = async_broadcast::broadcast(1);
-    let persistence = connect_persistence(
-        config.db,
-        &config.db_spec,
-        ConnectPersistenceFlags {
-            require_ssl: !config.do_not_require_ssl,
-            allow_read_only: false,
-            skip_index_creation: false,
-        },
-        &config.name(),
-        runtime.clone(),
-        preempt_signal.clone(),
-    )
-    .await?;
+    let persistence = if config.role.is_leader() && !boot_as_follower {
+        RolePersistence::Leader(
+            connect_persistence(
+                config.db,
+                &config.db_spec,
+                ConnectPersistenceFlags {
+                    require_ssl: !config.do_not_require_ssl,
+                    allow_read_only: false,
+                    skip_index_creation: false,
+                },
+                &config.name(),
+                runtime.clone(),
+                preempt_signal.clone(),
+            )
+            .await?,
+        )
+    } else {
+        RolePersistence::Follower(
+            connect_persistence_reader(
+                config.db,
+                &config.db_spec,
+                !config.do_not_require_ssl,
+                false,
+                &config.name(),
+                runtime.clone(),
+            )
+            .await?,
+        )
+    };
     let st = make_app(
         runtime.clone(),
         config.clone(),
@@ -163,13 +233,13 @@ async fn run_server_inner(runtime: ProdRuntime, config: LocalConfig) -> anyhow::
         preempt_signal.clone(),
     )
     .await?;
-    let router = router(st.clone());
+    let router = follower_router(st.clone());
     let mut shutdown_rx_ = shutdown_rx.clone();
     let http_service = ConvexHttpService::new(
         router,
         "backend",
         SERVER_VERSION_STR.to_string(),
-        MAX_CONCURRENT_REQUESTS,
+        *common::knobs::HTTP_SERVER_MAX_CONCURRENT_REQUESTS,
         *HTTP_SERVER_TIMEOUT_DURATION,
         HttpActionRouteMapper,
     );
@@ -199,8 +269,8 @@ async fn run_server_inner(runtime: ProdRuntime, config: LocalConfig) -> anyhow::
             force_exit_duration = Some(Duration::from_secs(0));
             let _: Result<_, _> = shutdown_tx.broadcast(()).await;
         }
-        r = signal::ctrl_c().fuse() => {
-            tracing::info!("Received Ctrl-C signal!");
+        r = shutdown_signal().fuse() => {
+            tracing::info!("Received shutdown signal!");
             r?;
             let _: Result<_, _> = shutdown_tx.broadcast(()).await;
         },
@@ -242,8 +312,8 @@ async fn run_server_inner(runtime: ProdRuntime, config: LocalConfig) -> anyhow::
                 tracing::info!("Cool down expired. Shutting down");
                 break;
             }
-            // Forcibly shutdown with second ctrl-c.
-            r = signal::ctrl_c().fuse() => {
+            // Forcibly shutdown with a second signal.
+            r = shutdown_signal().fuse() => {
                 r?;
                 tracing::warn!("Forcibly shutting down!");
                 break;
diff --git a/crates/local_backend/src/node_action_callbacks.rs b/crates/local_backend/src/node_action_callbacks.rs
index 22d9c58bb..a81c608ac 100644
--- a/crates/local_backend/src/node_action_callbacks.rs
+++ b/crates/local_backend/src/node_action_callbacks.rs
@@ -10,10 +10,14 @@ use axum::{
     RequestPartsExt,
 };
 use common::{
+    bootstrap_model::components::handles::FunctionHandle,
     components::{
+        CanonicalizedComponentFunctionPath,
         ComponentFunctionPath,
         ComponentId,
+        ComponentPath,
         PublicFunctionPath,
+        SerializedComponentFunctionPath,
     },
     execution_context::{
         ClientIp,
@@ -34,6 +38,11 @@ use common::{
         },
         ExtractClientVersion,
         HttpResponseError,
+        CONVEX_ACTIONS_CALLBACK_TOKEN_HEADER,
+        CONVEX_ACTION_FUNCTION_NAME_HEADER,
+        CONVEX_EXECUTION_ID_HEADER,
+        CONVEX_REQUEST_ID_HEADER,
+        CONVEX_ROOT_REQUEST_HEADER,
     },
     knobs::ACTION_USER_TIMEOUT,
     runtime::UnixTimestamp,
@@ -49,6 +58,8 @@ use fastrace::future::FutureExt;
 use http::HeaderMap;
 use isolate::UdfArgsJson;
 use keybroker::Identity;
+use model::file_storage::types::FileStorageEntry;
+use prost::Message;
 use serde::{
     Deserialize,
     Serialize,
@@ -85,6 +96,7 @@ use crate::{
 #[serde(rename_all = "camelCase")]
 pub struct NodeCallbackUdfPostRequest {
     pub path: Option<String>,
+    pub component_path: Option<String>,
     pub reference: Option<String>,
     pub function_handle: Option<String>,
     pub args: UdfArgsJson,
@@ -92,6 +104,38 @@ pub struct NodeCallbackUdfPostRequest {
     pub format: Option<String>,
 }
 
+async fn callback_function_path(
+    st: &LocalAppState,
+    identity: Identity,
+    component_id: ComponentId,
+    component_path: Option<String>,
+    path: Option<String>,
+    reference: Option<String>,
+    function_handle: Option<String>,
+) -> anyhow::Result<CanonicalizedComponentFunctionPath> {
+    let Some(component_path) = component_path else {
+        return st
+            .application
+            .canonicalized_function_path(identity, component_id, path, reference, function_handle)
+            .await;
+    };
+    anyhow::ensure!(
+        reference.is_none() && function_handle.is_none(),
+        ErrorMetadata::bad_request(
+            "InvalidUdfPathOrFunctionReference",
+            "componentPath cannot be combined with a function reference or handle."
+        )
+    );
+    let udf_path = path.context(ErrorMetadata::bad_request(
+        "MissingUdfPath",
+        "componentPath requires a UDF path.",
+    ))?;
+    Ok(CanonicalizedComponentFunctionPath {
+        component: ComponentPath::deserialize(Some(&component_path))?,
+        udf_path: udf_path.parse()?,
+    })
+}
+
 /// This is like `public_query_post`, except it allows calling internal
 /// functions as well. This should not be used for any publicly accessible
 /// endpoints, and should only be used to support Convex functions calling into
@@ -107,16 +151,16 @@ pub async fn internal_query_post(
     ExtractExecutionContext(context): ExtractExecutionContext,
     Json(req): Json<NodeCallbackUdfPostRequest>,
 ) -> Result<impl IntoResponse, HttpResponseError> {
-    let path = st
-        .application
-        .canonicalized_function_path(
-            identity.clone(),
-            component_id,
-            req.path,
-            req.reference,
-            req.function_handle,
-        )
-        .await?;
+    let path = callback_function_path(
+        &st,
+        identity.clone(),
+        component_id,
+        req.component_path,
+        req.path,
+        req.reference,
+        req.function_handle,
+    )
+    .await?;
     let udf_return = st
         .application
         .read_only_udf(
@@ -161,16 +205,16 @@ pub async fn internal_mutation_post(
     ExtractExecutionContext(context): ExtractExecutionContext,
     Json(req): Json<NodeCallbackUdfPostRequest>,
 ) -> Result<impl IntoResponse, HttpResponseError> {
-    let path = st
-        .application
-        .canonicalized_function_path(
-            identity.clone(),
-            component_id,
-            req.path,
-            req.reference,
-            req.function_handle,
-        )
-        .await?;
+    let path = callback_function_path(
+        &st,
+        identity.clone(),
+        component_id,
+        req.component_path,
+        req.path,
+        req.reference,
+        req.function_handle,
+    )
+    .await?;
     let udf_result = st
         .application
         .mutation_udf(
@@ -220,16 +264,16 @@ pub async fn internal_action_post(
     ExtractExecutionContext(context): ExtractExecutionContext,
     Json(req): Json<NodeCallbackUdfPostRequest>,
 ) -> Result<impl IntoResponse, HttpResponseError> {
-    let path = st
-        .application
-        .canonicalized_function_path(
-            identity.clone(),
-            component_id,
-            req.path,
-            req.reference,
-            req.function_handle,
-        )
-        .await?;
+    let path = callback_function_path(
+        &st,
+        identity.clone(),
+        component_id,
+        req.component_path,
+        req.path,
+        req.reference,
+        req.function_handle,
+    )
+    .await?;
     let udf_result = st
         .application
         .action_udf(
@@ -268,6 +312,7 @@ pub struct ScheduleJobRequest {
     reference: Option<String>,
     function_handle: Option<String>,
     udf_path: Option<String>,
+    component_path: Option<String>,
     udf_args: UdfArgsJson,
     scheduled_ts: f64,
 }
@@ -291,19 +336,17 @@ pub async fn schedule_job(
         ErrorMetadata::bad_request("InvalidTimestamp", "Requested scheduled_ts is invalid")
     })?;
     // User might have entered an invalid path, so this is a developer error.
-    let path = st
-        .application
-        .canonicalized_function_path(
-            identity.clone(),
-            component_id,
-            req.udf_path,
-            req.reference,
-            req.function_handle,
-        )
-        .await
-        .map_err(|e| {
-            anyhow::anyhow!(ErrorMetadata::bad_request("InvalidUdfPath", e.to_string()))
-        })?;
+    let path = callback_function_path(
+        &st,
+        identity.clone(),
+        component_id,
+        req.component_path,
+        req.udf_path,
+        req.reference,
+        req.function_handle,
+    )
+    .await
+    .map_err(|e| anyhow::anyhow!(ErrorMetadata::bad_request("InvalidUdfPath", e.to_string())))?;
     let udf_args = req.udf_args.into_serialized_args()?;
     let job_id = st
         .application
@@ -351,6 +394,7 @@ pub async fn cancel_developer_job(
 #[serde(rename_all = "camelCase")]
 pub struct CreateFunctionHandleRequest {
     udf_path: Option<String>,
+    component_path: Option<String>,
     reference: Option<String>,
 }
 
@@ -368,19 +412,17 @@ pub async fn create_function_handle(
     }: ExtractActionIdentity,
     Json(req): Json<CreateFunctionHandleRequest>,
 ) -> Result<impl IntoResponse, HttpResponseError> {
-    let path = st
-        .application
-        .canonicalized_function_path(
-            identity.clone(),
-            component_id,
-            req.udf_path,
-            req.reference,
-            None,
-        )
-        .await
-        .map_err(|e| {
-            anyhow::anyhow!(ErrorMetadata::bad_request("InvalidUdfPath", e.to_string()))
-        })?;
+    let path = callback_function_path(
+        &st,
+        identity.clone(),
+        component_id,
+        req.component_path,
+        req.udf_path,
+        req.reference,
+        None,
+    )
+    .await
+    .map_err(|e| anyhow::anyhow!(ErrorMetadata::bad_request("InvalidUdfPath", e.to_string())))?;
     let handle = st
         .application
         .runner()
@@ -538,6 +580,95 @@ pub async fn storage_delete(
     Ok(Json(json!(null)))
 }
 
+#[derive(Serialize, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub struct StorageGetFileEntryResponse {
+    component_path: String,
+    entry_proto: Vec<u8>,
+}
+
+pub async fn storage_get_file_entry(
+    MtState(st): MtState<LocalAppState>,
+    ExtractActionIdentity {
+        identity,
+        component_id,
+    }: ExtractActionIdentity,
+    Json(req): Json<GetParams>,
+) -> Result<impl IntoResponse, HttpResponseError> {
+    let storage_id = req.storage_id.parse()?;
+    let entry = st
+        .application
+        .runner()
+        .storage_get_file_entry(identity, component_id, storage_id)
+        .await?
+        .map(|(component_path, entry)| {
+            let entry_proto: pb::storage::FileStorageEntry = entry.into();
+            StorageGetFileEntryResponse {
+                component_path: String::from(component_path),
+                entry_proto: entry_proto.encode_to_vec(),
+            }
+        });
+    Ok(Json(entry))
+}
+
+#[derive(Serialize, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub struct StorageStoreFileEntryRequest {
+    entry_proto: Vec<u8>,
+}
+
+#[derive(Serialize, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub struct StorageStoreFileEntryResponse {
+    component_path: String,
+    document_id: String,
+}
+
+pub async fn storage_store_file_entry(
+    MtState(st): MtState<LocalAppState>,
+    ExtractActionIdentity {
+        identity,
+        component_id,
+    }: ExtractActionIdentity,
+    Json(req): Json<StorageStoreFileEntryRequest>,
+) -> Result<impl IntoResponse, HttpResponseError> {
+    let entry_proto =
+        pb::storage::FileStorageEntry::decode(&req.entry_proto[..]).map_err(anyhow::Error::from)?;
+    let entry = FileStorageEntry::try_from(entry_proto)?;
+    let (component_path, document_id) = st
+        .application
+        .runner()
+        .storage_store_file_entry(identity, component_id, entry)
+        .await?;
+    Ok(Json(StorageStoreFileEntryResponse {
+        component_path: String::from(component_path),
+        document_id: document_id.to_string(),
+    }))
+}
+
+#[derive(Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub struct LookupFunctionHandleRequest {
+    handle: String,
+}
+
+pub async fn lookup_function_handle(
+    MtState(st): MtState<LocalAppState>,
+    ExtractActionIdentity {
+        identity,
+        component_id: _,
+    }: ExtractActionIdentity,
+    Json(req): Json<LookupFunctionHandleRequest>,
+) -> Result<impl IntoResponse, HttpResponseError> {
+    let handle: FunctionHandle = req.handle.parse()?;
+    let path = st
+        .application
+        .lookup_function_handle(identity, handle)
+        .await?;
+    let serialized = SerializedComponentFunctionPath::try_from(path)?;
+    Ok(Json(serialized))
+}
+
 #[derive(Deserialize)]
 pub struct AuditLogParams {
     #[allow(dead_code)]
@@ -555,14 +686,12 @@ pub async fn audit_log(
     .into())
 }
 
-pub static CONVEX_ACTIONS_CALLBACK_TOKEN: &str = "Convex-Action-Callback-Token";
-
 async fn check_actions_token(
     st: &LocalAppState,
     headers: &HeaderMap,
 ) -> anyhow::Result<(SystemTime, ComponentId)> {
     let value = headers
-        .get(CONVEX_ACTIONS_CALLBACK_TOKEN)
+        .get(CONVEX_ACTIONS_CALLBACK_TOKEN_HEADER)
         .context("Missing callback token - is the call from actions?")?;
 
     let token = value
@@ -654,7 +783,7 @@ impl<S: Sync> FromRequestParts<S> for ExtractActionName {
     ) -> Result<Self, Self::Rejection> {
         let action_name = parts
             .headers
-            .get("Convex-Action-Function-Name")
+            .get(CONVEX_ACTION_FUNCTION_NAME_HEADER)
             .map(|value| value.to_str())
             .transpose()
             .context("Convex-Action-Function-Name must be a string")?
@@ -675,7 +804,7 @@ impl<T: Sync> FromRequestParts<T> for ExtractExecutionContext {
     ) -> Result<Self, Self::Rejection> {
         let request_id: RequestId = parts
             .headers
-            .get("Convex-Request-Id")
+            .get(CONVEX_REQUEST_ID_HEADER)
             .map(|v| v.to_str())
             .transpose()
             .context("Request id must be a string")?
@@ -685,7 +814,7 @@ impl<T: Sync> FromRequestParts<T> for ExtractExecutionContext {
             .unwrap_or(RequestId::new());
         let execution_id: ExecutionId = parts
             .headers
-            .get("Convex-Execution-Id")
+            .get(CONVEX_EXECUTION_ID_HEADER)
             .map(|v| v.to_str())
             .transpose()
             .context("Execution id must be a string")?
@@ -694,7 +823,7 @@ impl<T: Sync> FromRequestParts<T> for ExtractExecutionContext {
             // For backwards compatibility
             .unwrap_or(ExecutionId::new());
 
-        let is_root: bool = match parts.headers.get("Convex-Root-Request") {
+        let is_root: bool = match parts.headers.get(CONVEX_ROOT_REQUEST_HEADER) {
             Some(v) => v.to_str().context("Convex-Root-Request must be a string")? == "true",
             None => false,
         };
diff --git a/crates/local_backend/src/proxy.rs b/crates/local_backend/src/proxy.rs
index 0ea3363be..d401785cc 100644
--- a/crates/local_backend/src/proxy.rs
+++ b/crates/local_backend/src/proxy.rs
@@ -1,6 +1,10 @@
-use std::net::SocketAddr;
+use std::{
+    net::SocketAddr,
+    sync::LazyLock,
+};
 
 use axum::{
+    body::Body,
     extract::{
         Request,
         State,
@@ -15,9 +19,21 @@ use common::{
         HttpResponseError,
         NoopRouteMapper,
     },
-    knobs::HTTP_SERVER_TIMEOUT_DURATION,
+    knobs::{
+        HTTP_SERVER_MAX_CONCURRENT_REQUESTS,
+        HTTP_SERVER_TIMEOUT_DURATION,
+    },
 };
-use hyper_util::rt::TokioExecutor;
+use hyper_util::{
+    client::legacy::{
+        connect::HttpConnector,
+        Client,
+    },
+    rt::TokioExecutor,
+};
+
+static PROXY_CLIENT: LazyLock<Client<HttpConnector, Body>> =
+    LazyLock::new(|| Client::builder(TokioExecutor::new()).build_http());
 
 /// Routes HTTP actions to the main webserver
 pub async fn dev_site_proxy(
@@ -37,8 +53,7 @@ pub async fn dev_site_proxy(
     ) -> Result<impl IntoResponse, HttpResponseError> {
         let new_uri = format!("{}{}", site_forward_prefix, request.uri());
         *request.uri_mut() = new_uri.parse().map_err(anyhow::Error::new)?;
-        let resp = hyper_util::client::legacy::Client::builder(TokioExecutor::new())
-            .build_http()
+        let resp = PROXY_CLIENT
             .request(request)
             .await
             .map_err(anyhow::Error::new)?;
@@ -60,7 +75,7 @@ pub async fn dev_site_proxy(
         Router::new().fallback_service(router),
         "backend_http_proxy",
         "unknown".to_string(),
-        4,
+        *HTTP_SERVER_MAX_CONCURRENT_REQUESTS,
         *HTTP_SERVER_TIMEOUT_DURATION,
         NoopRouteMapper,
     );
