diff --git a/crates/common/src/persistence/mod.rs b/crates/common/src/persistence/mod.rs
index 5ec3445b3..bc1baef9e 100644
--- a/crates/common/src/persistence/mod.rs
+++ b/crates/common/src/persistence/mod.rs
@@ -326,6 +326,26 @@ pub trait Persistence: Sync + Send + 'static {
     async fn finish_loading(&self) -> anyhow::Result<()> {
         Ok(())
     }
+
+    fn supports_lease(&self) -> bool {
+        false
+    }
+
+    fn lease_ts(&self) -> Option<u64> {
+        None
+    }
+
+    async fn lease_held(&self) -> anyhow::Result<bool> {
+        anyhow::ensure!(
+            !self.supports_lease(),
+            "lease-capable persistence must implement lease_held"
+        );
+        Ok(true)
+    }
+
+    async fn release_lease(&self) -> anyhow::Result<bool> {
+        Ok(false)
+    }
 }
 
 #[derive(Debug, Clone, Copy)]
@@ -605,7 +625,6 @@ pub trait PersistenceReader: Send + Sync + 'static {
     async fn table_size_stats(&self) -> anyhow::Result<Vec<PersistenceTableSize>> {
         Ok(vec![])
     }
-
 }
 
 /// Timestamp that is repeatable because the caller is holding the lease and
diff --git a/crates/postgres/src/lib.rs b/crates/postgres/src/lib.rs
index 0d0691d42..d733dbf16 100644
--- a/crates/postgres/src/lib.rs
+++ b/crates/postgres/src/lib.rs
@@ -468,6 +468,41 @@ impl PostgresPersistence {
             .await?
             .is_none())
     }
+
+    pub async fn schema_initialized(
+        pool: Arc<ConvexPgPool>,
+        options: PostgresReaderOptions,
+    ) -> anyhow::Result<bool> {
+        let schema = match &options.schema {
+            Some(s) => SchemaName::new(s)?,
+            None => SchemaName::new(&get_current_schema(&pool).await?)?,
+        };
+        let client = pool
+            .get_connection("schema_initialized", &schema, &options.instance_name)
+            .await?;
+        if client
+            .query_opt(sql::CHECK_LEASES_TABLE_SQL, &[])
+            .await?
+            .is_none()
+        {
+            return Ok(false);
+        }
+        let mut params = vec![];
+        if options.multitenant {
+            params.push(&options.instance_name.raw as &(dyn ToSql + Sync));
+        }
+        if client
+            .query_opt(sql::check_lease_row_exists(options.multitenant), &params)
+            .await?
+            .is_none()
+        {
+            return Ok(false);
+        }
+        Ok(client
+            .query_opt(sql::check_newly_created(options.multitenant), &params)
+            .await?
+            .is_some())
+    }
 }
 
 #[async_trait]
@@ -524,6 +559,22 @@ impl Persistence for PostgresPersistence {
         })
     }
 
+    fn supports_lease(&self) -> bool {
+        true
+    }
+
+    fn lease_ts(&self) -> Option<u64> {
+        Some(self.lease.lease_ts() as u64)
+    }
+
+    async fn lease_held(&self) -> anyhow::Result<bool> {
+        self.lease.lease_held().await
+    }
+
+    async fn release_lease(&self) -> anyhow::Result<bool> {
+        self.lease.release().await
+    }
+
     #[fastrace::trace]
     async fn write<'a>(
         &self,
@@ -751,6 +802,10 @@ impl Persistence for PostgresPersistence {
         &self,
         mut documents: BoxStream<'_, Vec<DocumentLogEntry>>,
     ) -> anyhow::Result<()> {
+        anyhow::ensure!(
+            self.lease.lease_held().await?,
+            "import_documents_batch requires the lease (COPY bypasses the commit fence)"
+        );
         let conn = self
             .lease
             .pool
@@ -812,7 +867,10 @@ impl Persistence for PostgresPersistence {
             writer.finish().await?;
             break;
         }
-
+        anyhow::ensure!(
+            self.lease.lease_held().await?,
+            "lease lost during import_documents_batch; imported rows are not fenced"
+        );
         Ok(())
     }
 
@@ -820,6 +878,10 @@ impl Persistence for PostgresPersistence {
         &self,
         mut indexes: BoxStream<'_, Vec<PersistenceIndexEntry>>,
     ) -> anyhow::Result<()> {
+        anyhow::ensure!(
+            self.lease.lease_held().await?,
+            "import_indexes_batch requires the lease (COPY bypasses the commit fence)"
+        );
         let conn = self
             .lease
             .pool
@@ -878,7 +940,10 @@ impl Persistence for PostgresPersistence {
             writer.finish().await?;
             break;
         }
-
+        anyhow::ensure!(
+            self.lease.lease_held().await?,
+            "lease lost during import_indexes_batch; imported rows are not fenced"
+        );
         Ok(())
     }
 
@@ -1782,27 +1847,27 @@ impl Lease {
             .as_nanos() as i64;
 
         tracing::info!("attempting to acquire lease");
-        let stmt = client
+        let instance_name_q = instance_name.clone();
+        let row = client
             .with_retry(async move |client| {
-                client.prepare_cached(sql::lease_acquire(multitenant)).await
+                let mut params = vec![&ts as &(dyn ToSql + Sync)];
+                if multitenant {
+                    params.push(&instance_name_q.raw as &(dyn ToSql + Sync));
+                }
+                client
+                    .query_opt(sql::lease_acquire(multitenant), params.as_slice())
+                    .await
             })
             .await?;
-        let mut params = vec![&ts as &(dyn ToSql + Sync)];
-        if multitenant {
-            params.push(&instance_name.raw as &(dyn ToSql + Sync));
-        }
-        let rows_modified = client.execute(&stmt, params.as_slice()).await?;
         drop(client);
-        anyhow::ensure!(
-            rows_modified == 1,
-            "failed to acquire lease: Already acquired with higher timestamp"
-        );
-        tracing::info!("lease acquired with ts {}", ts);
-
+        let acquired_ts: i64 = row
+            .context("failed to acquire lease: leases row missing")?
+            .get(0);
+        tracing::info!("lease acquired with ts {}", acquired_ts);
         timer.finish();
         Ok(Self {
             pool,
-            lease_ts: ts,
+            lease_ts: acquired_ts,
             schema: schema.clone(),
             instance_name,
             multitenant,
@@ -1810,6 +1875,61 @@ impl Lease {
         })
     }
 
+    fn lease_ts(&self) -> i64 {
+        self.lease_ts
+    }
+
+    async fn lease_held(&self) -> anyhow::Result<bool> {
+        let lease_ts = self.lease_ts;
+        let mut client = self
+            .pool
+            .get_connection("lease_held", &self.schema, &self.instance_name)
+            .await?;
+        let multitenant = self.multitenant;
+        let instance_name = self.instance_name.clone();
+        let row = client
+            .with_retry(async move |client| {
+                let mut params = vec![&lease_ts as &(dyn ToSql + Sync)];
+                if multitenant {
+                    params.push(&instance_name.raw as &(dyn ToSql + Sync));
+                }
+                client
+                    .query_opt(sql::advisory_lease_check(multitenant), params.as_slice())
+                    .await
+            })
+            .await?;
+        Ok(row.is_some())
+    }
+
+    async fn release(&self) -> anyhow::Result<bool> {
+        let held_ts = self.lease_ts;
+        let mut client = self
+            .pool
+            .get_connection("lease_release", &self.schema, &self.instance_name)
+            .await?;
+        let multitenant = self.multitenant;
+        let instance_name = self.instance_name.clone();
+        let rows_modified = client
+            .with_retry(async move |client| {
+                let stmt = client
+                    .prepare_cached(sql::lease_release(multitenant))
+                    .await?;
+                let mut params = vec![&held_ts as &(dyn ToSql + Sync)];
+                if multitenant {
+                    params.push(&instance_name.raw as &(dyn ToSql + Sync));
+                }
+                client.execute(&stmt, params.as_slice()).await
+            })
+            .await?;
+        if rows_modified == 1 {
+            tracing::info!("lease released (ts {held_ts} invalidated); peer may take over");
+            Ok(true)
+        } else {
+            tracing::info!("lease release no-op: already lost (held ts {held_ts})");
+            Ok(false)
+        }
+    }
+
     /// Execute the transaction function f atomically ensuring that the lease is
     /// still held, otherwise return `LeaseLostError`.
     ///
diff --git a/crates/postgres/src/sql.rs b/crates/postgres/src/sql.rs
index a531a8b5a..de688877c 100644
--- a/crates/postgres/src/sql.rs
+++ b/crates/postgres/src/sql.rs
@@ -716,6 +716,20 @@ pub const fn unset_read_only(multitenant: bool) -> &'static str {
     )
 }
 
+pub(crate) const CHECK_LEASES_TABLE_SQL: &str =
+    r"SELECT 1 WHERE to_regclass('@db_name.leases') IS NOT NULL";
+
+pub const fn check_lease_row_exists(multitenant: bool) -> &'static str {
+    tableify!(
+        multitenant,
+        formatcp!(
+            "SELECT 1 FROM @db_name.leases WHERE {column}={value} LIMIT 1",
+            column = if multitenant { "instance_name" } else { "id" },
+            value = if multitenant { "$1" } else { "1" }
+        )
+    )
+}
+
 // If this query returns a result, the lease is still valid and will remain so
 // until the end of the transaction.
 pub const fn lease_precond(multitenant: bool) -> &'static str {
@@ -742,12 +756,22 @@ pub const fn advisory_lease_check(multitenant: bool) -> &'static str {
     )
 }
 
-// Acquire the lease unless acquire by someone with a higher timestamp.
 pub const fn lease_acquire(multitenant: bool) -> &'static str {
     tableify!(
         multitenant,
         formatcp!(
-            "UPDATE @db_name.leases SET ts=$1 WHERE {column}={value} AND ts<$1",
+            "UPDATE @db_name.leases SET ts=GREATEST($1, ts+1) WHERE {column}={value} RETURNING ts",
+            column = if multitenant { "instance_name" } else { "id" },
+            value = if multitenant { "$2" } else { "1" }
+        )
+    )
+}
+
+pub const fn lease_release(multitenant: bool) -> &'static str {
+    tableify!(
+        multitenant,
+        formatcp!(
+            "UPDATE @db_name.leases SET ts=ts+1 WHERE {column}={value} AND ts=$1",
             column = if multitenant { "instance_name" } else { "id" },
             value = if multitenant { "$2" } else { "1" }
         )
